#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll n,a[200001],ans,mmin=LLONG_MAX;
vector<int> ed[1000001];

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin>>n;
    ll base=0;
    cin>>a[1];

    for (int i=2; i<=n; ++i) {
        cin>>a[i];
        if (a[i]==v.back())
    }



}
