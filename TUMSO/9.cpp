#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

struct A{
    ll x,y;
} p[100001];

int n;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin>>n;
    for (int i=0; i<n; ++i) cin>>p[i].x>>p[i].y;

}

